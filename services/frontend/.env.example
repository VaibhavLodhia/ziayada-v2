# Production (Vercel): set in Project → Settings → Environment Variables, then redeploy.
# Example: https://ziayada-api.railway.app  (no trailing slash)
VITE_API_BASE_URL=

# Local dev only — Vite proxy target
BACKEND_URL=http://localhost:8001
